name: Module 3 Adaptive CI

on:
  push:
    paths:
      - 'src/module-3/**'
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print uname
        run: uname -a
